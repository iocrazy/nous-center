name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  backend:
    name: Backend · lint + tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Sync deps (skip vllm on CI — CPU-only runner)
        run: uv sync --frozen --no-install-package vllm

      - name: Lint (ruff)
        run: uv run ruff check src tests

      - name: Tests (forked per-file to survive segfaults)
        # `--forked` runs each test in a subprocess so a SIGSEGV in one file
        # (heavy GPU-adjacent imports) does not kill the whole run.
        # Integration smoke needs a live vLLM and is excluded from CI.
        run: |
          uv run pytest -q --forked -p no:cacheprovider \
            --ignore=tests/test_integration_smoke.py \
            --ignore=tests/test_tts_engine_adapter.py
        env:
          DATABASE_URL: "sqlite+aiosqlite:///:memory:"
          REDIS_URL: "redis://localhost:6379/0"
          CUDA_VISIBLE_DEVICES: ""

  frontend:
    name: Frontend · lint + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install Rust (for wasm-pack)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Install deps
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
